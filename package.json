{
  "name": "mouhamed-sall-portfolio-spa",
  "version": "1.0.0",
  "private": true,
  "description": "SPA portfolio en Vanilla JavaScript avec json-server",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --static .",
    "server": "json-server --watch db.json --port 3000",
    "check": "node --check js/app.js && node --check js/api.js && node --check js/dom.js && node --check js/projets.js && node --check js/state.js"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
